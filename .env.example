# Names only. Never commit real values.

# Server
PORT=
# Owner-only access for a personal deployment (not yet enforced; see PR notes).
OWNER_ACCESS_TOKEN=

# Transcription provider (OpenAI-compatible endpoint).
# The request language is set to Hebrew in code, not here.
TRANSCRIPTION_API_KEY=
TRANSCRIPTION_BASE_URL=
TRANSCRIPTION_MODEL=

# Summarization provider.
SUMMARY_API_KEY=
SUMMARY_BASE_URL=
SUMMARY_MODEL=
